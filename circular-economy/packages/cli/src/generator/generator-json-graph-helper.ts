import type { CircularEconomy } from 'ceml-language';
import type { Graph } from './generator-json-types.js';

export class JsonGraphHelper {
    generateGraph(model: CircularEconomy): Graph {
        /**
         * TODO: Implement graph generation and return a {@link Graph} object.
         */
        throw new Error('Not implemented.')
    }
}

